# Core LLM & Agent frameworks
langchain
langchain-google-genai
langchain-openai
langchain-anthropic
langchain-community
langchain-tavily
langgraph

# Configuration & utilities
python-dotenv

# Web scraping & search
tavily-python
beautifulsoup4
httpx  # Async HTTP client (replaced requests for non-blocking I/O)
feedparser

# Scientific computing (removed - was only for kokoro TTS)

# Image processing
Pillow

# TTS engines (removed kokoro - using email service instead)
pyttsx3

# NLP
spacy

# API server
fastapi
uvicorn[standard]

# Celery & Redis (background task processing)
celery[redis]==5.3.4
redis>=4.5.2,<5.0.0  # Compatible with Celery 5.3.4
flower==2.0.1  # Celery monitoring dashboard

# Database
mongoengine
pymongo  # Required for GridFS support
dnspython  # Required for MongoDB+SRV connection strings

# Authentication & Security
python-jose[cryptography]  # JWT token handling
passlib[bcrypt]
argon2-cffi  # Password hashing
python-multipart  # For form data parsing
pydantic[email]  # Email validation
authlib  # OAuth library for Google authentication
